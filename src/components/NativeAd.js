import React from 'react';
import { View } from 'react-native';

// Native ads are temporarily disabled due to complexity
// Only banner ads are active for now
// TODO: Implement native ads properly when the library is more stable

const NativeAdComponent = ({ style }) => {
    // Return null - native ads disabled for stability
    return null;
};

export default NativeAdComponent;
